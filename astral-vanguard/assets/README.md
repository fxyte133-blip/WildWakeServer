# Assets

Astral Vanguard currently builds its characters, weapons, terrain, vegetation, particles, and interface procedurally from the source files in `src/`.

Place future imported models, textures, audio, and other source assets in this directory. Files that must be copied directly into the production build should be placed in `public/`.
